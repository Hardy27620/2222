#include <iostream>


using namespace std;

int main()
{
    int a,b,c;

    cin >> a >> b >> c;
    int s,sum;
    sum=(a+b+c)/2;

    s=sum*(sum-a)*(sum-b)*(sum-c);
    cout << s ;

    return 0;
}
